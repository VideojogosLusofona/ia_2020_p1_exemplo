using System;
using ColorShapeLinks.Common;
using ColorShapeLinks.Common.AI;
using ColorShapeLinks.Common.Session;
using Ultron;

namespace TestUltron
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Work in progress");
            // MatchConfig mc = new MatchConfig();
            // ThinkerPrototype tp = new ThinkerPrototype(
            //     "Ultron.UltronThinker", "", mc);
            // IThinker thinker = tp.Create();
            // Console.WriteLine(thinker.ToString());
            // foreach (string thinker in ThinkerManager.Instance.ThinkerNames)
            //     Console.WriteLine(thinker);
        }
    }
}
